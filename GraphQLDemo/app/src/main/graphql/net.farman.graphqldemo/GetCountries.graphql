query GetCountries($filter: CountryFilterInput){
    countries(filter: $filter){
        code
        name
        currency
        capital
        emoji
        continent {
            name
            code
        }
    }
}